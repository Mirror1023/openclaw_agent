# Copy to .env and fill in values as needed.
# NEVER commit .env. It contains secrets.

# Mode B (OpenAI)
OPENAI_API_KEY="sk-REPLACE_ME"

# Optional: If your python is not `python3`, set the python executable that OpenClaw plugin tools should use.
# (Example: KB_PYTHON="/opt/homebrew/bin/python3")
KB_PYTHON="python3"
